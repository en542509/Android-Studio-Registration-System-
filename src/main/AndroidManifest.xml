<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yiweizhang.csci3130project">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentAccountActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity android:name=".LoginActivity"> -->
        <!-- </activity> -->

        <activity
            android:name=".AddDropActivity"
            android:label="@string/title_activity_login_page"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AccountActivity"
            android:label="AccountActivity"
            android:parentActivityName=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CourseAddActivity"
            android:label="CourseAddActivity"
            android:parentActivityName=".AccountActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CheckConflictActivity" />
        <activity android:name=".WeeklyScheduleActivity" />
        <activity android:name=".CourseListActivity" />
        <activity android:name=".AccountRegistrationActivity" />
        <activity android:name=".Instructor_mycourse"></activity>
        <activity android:name=".wlistActivity" />
        <activity android:name=".StudentAccountDetail" />
        <activity android:name=".Updatephoto" />
        <activity android:name=".SearchCourse" />
        <activity android:name=".CSCI" />
        <!-- <activity android:name=".StudentAccountActivity"> -->


        <!-- </activity> -->
        <!-- <service -->
        <!-- android:name=".MyUploadService" -->
        <!-- android:exported="false" /> -->
        <activity android:name=".MajorAndTermActivity"></activity>
    </application>

</manifest>